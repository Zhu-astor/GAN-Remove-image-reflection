# §4.6 改寫提案 — 2026-06-13

> 狀態：**草案，待使用者審閱**（依 CLAUDE.md §5.2，尚未套用至 `cvgip2025_chinese.py` / `.docx`）
> 對應任務：Task #2（FIG 素材規格）、Task #3（七處改寫草案中，實際需改動者僅 §4.6 + §5.4）、Task #4（引用評估）

---

## 1. 決策摘要（回應使用者 2026-06-12 指示）

| 項目 | 結論 |
|------|------|
| 92.7% → 94.5%（+1.8pp） | ✅ **維持沿用**（方案 A 取消）。來源已verified：`反光處理論文準備.pdf` §6（699 張測試集，原始 92.7%，40 張失敗中 10 張在去反光版成功 → 94.5%，人工計數，PAPER_STATUS.md 稱為「權威版本」）+ `FY113-機器學習了沒實證成果簡報v2.pptx` slide 11（514/554→524/554，同一組數字的另一種呈現） |
| 98.62% / 98.77%（CLASS_train8 × 651 張） | ❌ **丟棄**，依使用者明確指示：「之前98.62 98.77都是從錯誤的模型判斷閾值、條件 錯誤模型出來的」 |
| `cvgip2025_chinese.py` 現況檢查 | **重要發現**：檔案目前 8 處（行 182, 209, 249, 499, 514, 591-592, 689）**已經是** 92.7%/94.5%/699 張，方案 A 從未實際套用到 .py。因此**這 8 處不需要任何數字改動**——唯一需要修改的是 §4.6 本文敘事結構 + Table 2（含 [MUST-3] 佔位符）+ FIG-6。 |
| N=554（PPTX）vs N=699（PDF） | 沿用現有 .py 的 699 張框架（與 PDF §6 一致，且已是論文現有寫法），PPTX 的 514/554→524/554 僅作為佐證來源，不寫入正文，避免兩套分母並陳造成混淆。 |

---

## 2. FIG 素材規格（Task #2 — 請使用者協助尋找）

新敘事不再以「Baseline Pix2Pix vs SGA 下游準確率」數值比較為主軸，而是優先呈現**人眼可辨的反光消除效果**，再輔以 92.7→94.5 的量化結果。對應地，§4.6 需要一張新的展示圖，取代目前的 `[FIG-6 — downstream_accuracy.png]`（bar chart 佔位符，原為方案 A 設計，現已不適用）。

### 需要的素材類型（任一即可，多個更好）

1. **YOLO 偵測信心值對比圖（優先）**
   - 內容：同一張博物館展品影像的「處理前／處理後」YOLOv8 偵測結果，畫面上需可見 bounding box + class + confidence 標籤。
   - PAPER_STATUS.md 已記錄一組現成候選：`消跑原7.jpg`（未經 SGA） / `原跑原7.jpg`（經 SGA 處理）——3 個物件信心值皆提升（0.76→0.93, 0.48→0.85, 0.64→0.81）。
   - **請確認**：這兩個檔案目前的完整路徑（在 `D:\Contest\AI GO\` 下的哪個子目錄），以及是否還有其他類似的「信心值提升」或「未偵測→成功偵測」的範例圖。
   - 若有「未偵測 → 成功偵測」的案例（即原始影像該物件 conf < 門檻完全沒框，SGA 處理後出現框），會比「信心值提升」更貼合 92.7%→94.5% 的「40 張失敗中 10 張救回」敘事，是最理想的單例。

2. **一般視覺比較（強化版，輔助選項）**
   - 現有 `matherial/visual_comparison.png`（5 組 Original/Generated）已用於 §4.4 圖 3。
   - 若手上有反光消除效果**更明顯**（人眼一看就覺得「反光真的不見了」）的額外組別，可提供作為 §4.6 開場視覺證據的補充或替換。

### 格式要求
- 圖片本身可以是已經畫好框＋信心值標籤的成品圖（不需要我重新跑模型）。
- 若提供的是「原圖」而非「已標註圖」，請同時告知對應的 YOLO checkpoint 名稱（例如 `saved_model_001`），以便我用同一模型重跑並產生標註版本——但**僅在使用者指定 checkpoint 後**才會執行，避免重蹈先前「不確定哪個模型代表 92.7/94.5 評估」的問題。

---

## 3. §4.6 改寫草案（Task #3）

> 對應 `cvgip2025_chinese.py` 第 585–605 行（`h2(doc, '4.6. 跨場景下游辨識結果')` 至 `圖 6` caption 結束）。
> 以下為**完整替換草案**，章節標題微調以反映新敘事重點。

```python
h2(doc, '4.6. 跨場景下游效益：視覺證據與辨識準確率')
p(doc,
  '圖 3（§4.4）已呈現博物館藏品在 Pix2Pix+SGA 處理前後的視覺比較：'
  '反光區域明顯減弱，展品表面材質紋理與邊緣細節的可辨識度提升，'
  '此差異對人眼而言相當直觀，是本文最直接的效果證據。'
  '圖 6 進一步以 YOLOv8 辨識結果為例，呈現同一展品影像在反光消除前後的'
  '偵測框與信心值變化：[待 FIG 素材確認後填入具體數值，例如'
  '「objX 信心值由 0.76 提升至 0.93」]，顯示反光消除對下游辨識任務的'
  '直接增益。')
p(doc,
  '在量化層面，本文以博物館評估集（699 張，涵蓋陶瓷器、書法畫作、'
  '金屬文物及立體雕塑等 7 類展品）測試 YOLOv8 辨識結果：'
  '原始含反光影像的辨識準確率為 92.7%（699 張中 40 張未能成功辨識）；'
  '經 Pix2Pix+SGA 反光消除後，此 40 張中有 10 張（25%）轉為成功辨識，'
  '整體準確率提升至 94.5%（+1.8 pp）。')
p(doc,
  '整體準確率提升幅度（+1.8 pp）相對有限，我們推測主要受兩項因素制約：'
  '（1）可改善的失敗樣本基數本身有限——699 張中僅 40 張（5.7%）原始辨識'
  '失敗，理論上即使全部救回，準確率上限亦僅能提升至 5.7 個百分點'
  '（即 100%）；以「失敗案例救回率」衡量，本文實際救回 10/40（25%），'
  '顯示反光消除對「邊緣案例」具有實質助益。'
  '（2）辨識模型本身在原始影像上已達 92.7% 的高基準準確率，'
  '可改善空間天生受限，使反光消除的邊際貢獻不易在整體指標中充分顯現。'
  '此外，本文博物館評估集規模（699 張、7 類）相較於大型通用偵測基準'
  '（如 COCO）偏小，單一類別樣本數有限，使整體準確率對個別案例的'
  '敏感度較高。這些限制將於 §5.4 進一步討論，並列為未來工作方向'
  '（更大規模、更多類別、原始準確率較低之跨場景下游評估）。',
  indent=True)
caption(doc,
  '表 2. 跨場景下游辨識準確率（博物館評估集，699 張，7 類展品）。\n'
  '\n'
  '條件                          | 準確率 (%) | 說明\n'
  '──────────────────────────────|────────────|──────────────────────────\n'
  '原始影像（含反光）             | 92.7       | 699 張中 40 張未能成功辨識\n'
  'Pix2Pix + SGA（本文）         | 94.5       | +1.8 pp；40 張中 10 張（25%）救回')
fig(doc, '<待 FIG 素材確定後填入檔名>')
caption(doc,
  '圖 6. YOLOv8 辨識結果範例：原始含反光影像（左）與 Pix2Pix+SGA 處理後'
  '影像（右）之偵測框與信心值比較。'
  '[待 FIG 素材確定後填入具體案例與數值]')
```

### 與現行版本的差異說明
- **保留**：92.7% / 94.5% / +1.8pp / 699 張 / 7 類展品（均為現有 .py 已採用之數字，未變動）。
- **新增**：「40 張失敗中 10 張救回（25%）」的描述——此數字直接來自已驗證的 `反光處理論文準備.pdf` §6 原文（「40 張沒有成功辨識的照片在去反光測試集中有 10 張成功辨識」），純算術衍生（10/40=25%），無需額外引用。
- **新增**：一段「為何提升幅度有限」的脈絡化討論（對應使用者要求的「很多說明」），論證方式為**算術上界**（92.7%+5.7pp理論上限=100%，實際只用到 1.8/5.7）與**評估集規模對比**，不依賴未驗證的外部主張。
- **移除**：Table 2 中「Baseline Pix2Pix」列與 `[MUST-3]` 佔位符——因新敘事不再以「Baseline vs SGA 下游準確率」數值比較為論證主軸，此列已無存在必要，**[MUST-3] 因此直接解除，不需再執行 `eval_class_train8_651.py` 或任何額外評估腳本**。
- **移除**：圖 6 原本的 bar chart（`downstream_accuracy.png`，方案 A 設計），改為 YOLO 偵測信心值對比圖（待 Task #2 素材確認）。

---

## 4. §5.4 限制分析補充草案（Task #3）

> 對應 `cvgip2025_chinese.py` 第 651–661 行（`h2(doc, '5.4. 限制分析')` 段落），於現有三點限制之後**新增第四點**。

現有段落結尾為：「第三，本文尚未系統驗證強烈動態反光（如戶外強日照）的消除效果【9】。」
之後新增：

```python
p(doc,
  '第四，§4.6 的下游辨識效益（92.7%→94.5%，+1.8pp）之整體幅度受評估集'
  '規模與辨識模型基準準確率共同制約：699 張、7 類的評估集中僅 40 張'
  '（5.7%）原始辨識失敗，理論改善上限本身有限；同時辨識模型在原始影像上'
  '已具備 92.7% 的高基準準確率，使反光消除的邊際貢獻不易在整體指標中'
  '充分顯現。更大規模、更多類別、且原始辨識準確率較低（改善空間較大）的'
  '跨場景下游評估資料集，將是後續驗證本方法效益的重要方向。',
  indent=True)
```

---

## 5. 引用評估（Task #4）

| 草案中的論點 | 是否需要外部引用 | 說明 |
|---|---|---|
| 92.7% / 94.5% / 40 張 / 10 張 / 699 張 | ❌ 不需要 | 已透過 `反光處理論文準備.pdf` §6 verified（本文作者自身先前競賽成果），符合 §5.2 對「已驗證來源」之要求 |
| 10/40 = 25% 救回率 | ❌ 不需要 | 對已驗證數字的純算術運算 |
| 92.7% + 5.7pp = 100% 理論上限 | ❌ 不需要 | 對已驗證數字的純算術運算（準確率定義域為 [0,100%]） |
| 「699 張、7 類」相較大型基準資料集偏小 | ❌ 不需要 | 屬於常識性規模比較（COCO/ImageNet 等的規模為公開常識），非本文需驗證的特定主張 |
| 「高基準準確率→邊際貢獻不易顯現」之一般化推論 | ⚠️ 視深度而定 | 草案中已用「我們推測」+ 算術上界佐證的方式表述（與 PDF §6 原文「我們推測這與辨識模型的訓練成效以及訓練集的規模有關」語氣一致），屬於作者自身對自身實驗結果的詮釋討論，依 §5.2 不強制要求第三方引用。若使用者希望進一步以文獻佐證「小規模評估集 + 高基準準確率 → 可量測增益受限」這一更廣泛的現象，可另行透過 `/cite-papers` 搜尋 — **本提案暫不主動搜尋**，避免為一個算術上自明的論點附加可能不夠精確的引用 |

**結論**：草案內容可在不新增引用的情況下完整成立。若使用者認為仍需加強學術佐證，請告知欲強化的具體論點，再啟動 `/cite-papers` 流程。

---

## 6. 待辦與下一步

1. ⏳ **等待使用者**：提供 §2 所列 FIG 素材（`消跑原7.jpg`/`原跑原7.jpg` 路徑，或其他更合適的範例）。
2. ⏳ **等待使用者審閱**：本文件 §3、§4 草案文字（可逐句修改）。
3. 待 1、2 完成後：套用至 `cvgip2025_chinese.py`（§4.6 行 585-605、§5.4 行 651-661 後新增段落）、插入新 FIG-6、重新生成 `.docx`。
4. 依 `feedback_ai_go_github_push.md`：套用後將異動檔案複製到 `github/paper/` 並 push，回傳連結。
